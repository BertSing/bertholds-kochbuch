(()=>{const r=document.documentElement;const saved=localStorage.getItem('theme');if(saved)r.dataset.theme=saved;const toggle=document.querySelector('#themeToggle');toggle?.addEventListener('click',()=>{const next=r.dataset.theme==='dark'?'light':'dark';r.dataset.theme=next;localStorage.setItem('theme',next)});const menu=document.querySelector('#menuButton'),nav=document.querySelector('#mainNav');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});const cards=[...document.querySelectorAll('.recipe-card')],search=document.querySelector('#search'),filters=[...document.querySelectorAll('.filter')],cats=[...document.querySelectorAll('.category-card')],count=document.querySelector('#resultCount'),empty=document.querySelector('#emptyState');let active='alle';const norm=s=>(s||'').toLocaleLowerCase('de-DE').normalize('NFD').replace(/[\u0300-\u036f]/g,'');function update(){const term=norm(search?.value);let visible=0;cards.forEach(card=>{const okCat=active==='alle'||card.dataset.category===active;const okText=!term||norm(card.dataset.search).includes(term);card.hidden=!(okCat&&okText);if(!card.hidden)visible++});if(count)count.textContent=`${visible} ${visible===1?'Rezept':'Rezepte'}`;if(empty)empty.hidden=visible!==0}function setFilter(v){active=v;filters.forEach(b=>b.classList.toggle('active',b.dataset.filter===v));update();document.querySelector('#rezepte')?.scrollIntoView({behavior:'smooth'})}search?.addEventListener('input',update);filters.forEach(b=>b.addEventListener('click',()=>setFilter(b.dataset.filter)));cats.forEach(b=>b.addEventListener('click',()=>setFilter(b.dataset.filter)));update();if('serviceWorker'in navigator){navigator.serviceWorker.getRegistrations().then(rs=>rs.forEach(x=>x.unregister()));caches?.keys().then(keys=>keys.forEach(k=>caches.delete(k)))}})();